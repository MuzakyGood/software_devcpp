#include <iostream>
#include "SM/math.hpp"

int main(){

    // All Basic Math can call with this
    std::cout << plus(10, 40) << "\n"; // This is an example of a function Increments
    std::cout << min(40, 25) << "\n"; // This is an example of a function Subtraction
    std::cout << multi(5, 5) << "\n"; // This is an example of a function Multiplication
    std::cout << share(100, 5) << "\n"; // This is an example of a function Division

    return 0;
}
